# Backend env for Razorpay & FCM
PORT=5000
CORS_ORIGIN=http://localhost:3001,https://dashco-com.vercel.app
RAZORPAY_KEY_ID="rzp_test_RD3J1sajzD89a8"
RAZORPAY_KEY_SECRET="6lrzvFBe91tS3uR3OlD67hoP"

# Firebase Cloud Messaging (server-side send)
# Get this from Firebase console > Project settings > Cloud Messaging > Cloud Messaging API (Legacy)
FCM_SERVER_KEY=
# Optional: default admin device token to notify (can also be sent from client)
FCM_NOTIFY_TOKEN=

# Firebase Admin SDK (preferred). Provide ONE of the following credential inputs:
# 1) Inline JSON (one line, minified)
# FIREBASE_ADMIN_CREDENTIALS={"type":"service_account","project_id":"your-project",...}
# 2) Base64 of the full JSON (useful to avoid quoting issues)
# FIREBASE_ADMIN_CREDENTIALS_BASE64=
# 3) Absolute path to the downloaded service account JSON file
# FIREBASE_ADMIN_CREDENTIALS_PATH=
# Alternatively, set GOOGLE_APPLICATION_CREDENTIALS to the JSON path for ADC
# GOOGLE_APPLICATION_CREDENTIALS=

# FCM driver selection: admin (SDK), legacy (server key), auto, none
FCM_DRIVER=admin

# Email (Gmail via Nodemailer)
# Use an App Password (recommended) for Gmail: https://myaccount.google.com/apppasswords  
GMAIL_USER="yourgmail@gmail.com"    
GMAIL_APP_PASSWORD="your_app_password"  
# Destination inbox (default uses GMAIL_USER if not set)
MAIL_TO="yourgmail@gmail.com"
